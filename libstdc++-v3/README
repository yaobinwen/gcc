file: libstdc++-v3/README

New users may wish to point their web browsers to the file
index.html in the 'doc/html' subdirectory.  It contains brief
building instructions and notes on how to configure the library in
interesting ways.

---

- `include`:
  - `std`: All the standard header files.
  - `bits`: This directory seems to include implementation details which are
    distributed together with the standard headers. The `.tcc` files are
    actually C++ code.
